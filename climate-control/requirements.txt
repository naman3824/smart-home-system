# Dependencies for climate-control/api_server.py ONLY.
# Deliberately lean — this service just fetches OpenWeather data (httpx) and
# serves it over FastAPI. It does not use the main dashboard's `requests` or DB.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
